import React, { Component } from 'react';

export default class Tickets extends Component{

    render(){
        
        return(
            <p>Tickets component content placeholder to make sure this renders.</p>
        )
    }
}